{
  "name": "tourism-web-app",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm run dev"
  },
  "dependencies": {
    "mongoose": "^8.18.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
